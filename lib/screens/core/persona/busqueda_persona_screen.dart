import 'package:flutter/material.dart';

class BusquedaPersonaScreen extends StatelessWidget {
  static const String screenName = 'Busqueda';

  const BusquedaPersonaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
